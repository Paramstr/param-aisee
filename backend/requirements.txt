fastapi==0.111.*
uvicorn[standard]==0.30.*
openai>=1.0.0
sounddevice
mlx-whisper
pydantic
pydantic-settings
opencv-python-headless
pillow
python-dotenv
webrtcvad
# Removed problematic dependencies that caused memory corruption:
# - qwen-vl-utils (PyTorch tensor memory issues)
# - torch (not needed for CPU-only video processing)  
# - torchvision (deprecated video capabilities with memory bugs)
